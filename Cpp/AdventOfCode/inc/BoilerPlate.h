#pragma once

#include "Common.h"

std::string Run(const std::vector<std::string>& lines) {
    for (const auto& line : lines) {
        (void)line;
    }
    return "";
}

bool RunTests() {
    return true;
}